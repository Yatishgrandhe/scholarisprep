import { LearnSession } from "@/components/dashboard/vocab/LearnSession";

export const metadata = { title: "Learn Mode · Vocab" };

export default function VocabLearnPage() {
  return <LearnSession />;
}
