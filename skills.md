# Skills Log

## 2026-07-20
- **INTEGRATOR wave (own-shell + polish):** Waited ~5min for sibling agents; no merge-conflict markers. Coherent ship: FreeStudyStudioShell + LabsShell own chrome (dashboard fullscreen via `shellRoutes`); FS/Labs open in new tab (`PRODUCT_NEW_TAB_PROPS` / `external`); CSP keeps ORT `/ort/` + `wasm-unsafe-eval`; BoardPdfPeek removed (PDF stays Free Studying text path); BoardChatDock/voice polish; `tutor_conversations` CHECK allows `free_study` (+ client `queries.ts`); studio-tokens CSS; text-only Mistral preserved. `npm run build` exit 0. Commit+push `origin/master` (no secrets / no force). Leftovers: dual LabsShell vs LabsStudioShell (+ unused `free-study-shell` CSS twin); Next skips typecheck in build; pre-existing `tsc` noise (`course_id`, `question_subtype`, etc.); migration must be applied on remote Supabase.
- **Free Studying own-shell mobile (drawer + compact rail):** Hardened `FreeStudyStudioShell` / `FreeStudyProductRail` / `free-study-studio-shell.module.css` ≤860px — toggleable slide-out labeled drawer (`inert` when closed), compact icon strip (destinations + Whiteboard) with ≥44px targets, full labels in drawer, Esc closes drawer then exits. Desktop density ladder unchanged. No commit.
- **FreeStudyStudioShell (own-shell rail):** Built research-named `FreeStudyStudioShell` + `FreeStudyProductRail` + `free-study-studio-shell.module.css` — cobalt Notion/Figma destination rail (Tutor/PDF/Voice/Notes, Whiteboard same-tab, STEM Labs new-tab, Dashboard/Esc). Density ladder full→compact→hidden (PDF defaults compact). Wired `free-study/layout.tsx`; `FreeStudyShell` re-exports. a11y via `freeStudyA11y`. Whiteboard untouched. No commit.
- **Deep links with own shell (FS + Labs):** Fixed Free Studying `?dest=` / legacy `?mode=` when opened in new-tab own shell — hub now syncs mode from URL (`useSearchParams` + shared `parseDest.ts`); page remounts hub via `key={mode}`; studio shell canonicalizes `?mode=` → `?dest=`. Auth middleware preserves query in `redirectTo`. Labs: decode simId, `SimLabHost` keyed by id, catalog/sidebar use `LABS_HREF/${id}`, LabsShell active id decode. No commit.
- **STEM Labs own-shell mobile:** `LabsShell` ≤860px = off-canvas drawer (menu + title bar + scrim), body scroll lock, Esc/focus restore, `inert` closed rail + stage when open, ≥44px touch targets; ≤480px full-width drawer + 2-col categories. Catalog grid single-column on phones; `SimShell` stacks viewport/controls with thumb-safe predict actions. Wired layout still `/dashboard/labs`. No commit.
- **STEM Labs own-shell CSS polish:** Rewrote `labs-shell.module.css` (wired `LabsShell`) to match Free Studying / dashboard dark product rail (`--dashboard-sidebar-*` + `--shell-*`): cobalt inset active, 13px nav density, focus rings, dim rail vs bright cobalt stage. Kept mobile top bar + drawer (≤860). Added polished `labs-studio-shell.module.css` for `LabsStudioShell`/`LabsProductRail` (density ladder full→compact→hidden + peep). Catalog cards in `sims-hub.module.css`; `SimShell` viewport/rail/button chrome. `BrandLockup variant="light"` on dark rails. No commit.
- **Free Studying shell a11y:** Shared `freeStudyA11y.ts` (landmarks, destination labels, Arrow/Home/End). Wired into `FreeStudyShell` sidebar + compact rail (aria-current, current labels, Esc/Tab trap in drawer, inert when closed); `FreeStudyLanding` destinations; hub skip-to-workspace + chat live region + `:focus-visible` on hub chrome. Cobalt focus rings on shell nav/menu/compact/overlay. No commit.
- **Free Studying own-shell sidebar CSS polish:** Rewrote `free-study-shell.module.css` to match dashboard OnePrep navy rail (12px UI type, 1.75rem rows, cobalt inset active, `--space` / `--dashboard-sidebar-*` / `--shell-*` tokens). Desktop product sidebar + stage; mobile top bar + compact icon rail + slide-out drawer (`.sidebarOpen`). Quieted legacy studio `.rail` (no floating card). Workspace title → instrument sans. Hub is workspace-only via `focusedShell`. No commit.
- **Home CTAs new-tab + open cue (sibling FAIL fix):** Dashboard `/dashboard` hero Free Studying + STEM Labs are plain `<a {...PRODUCT_NEW_TAB_PROPS}>` with subtle `ArrowSquareOut` (`.ctaNewTabCue`). FreeStudyLanding “Open STEM Labs” same (`PRODUCT_NEW_TAB_PROPS` + `.labsOpenCue`). Coordinated with nav agent (`external: true` sidebar/mobile; TopBar `window.open`). No commit.
- **Whiteboard Studio after FS/Labs shell (parity):** Confirmed Scho open + AI dock not regressed — `BoardSchoOpen` → `schoOpenDone` → dock `revealReady`; Ask strip/toolbar still gated. Shell still hides via `isWhiteboardFullscreenRoute` (alongside free-study/labs). PDF peek already removed (PDF stays Free Studying). Parity fill: studio + whiteboard layout use `100dvh` like FreeStudyShell/LabsShell; dock composer `width: 100%`; `--shell-*` chrome tokens. `tsc` clean for WB paths. No commit.
- **STEM Labs shell a11y:** Added `labsA11y.ts` (`LABS_ARIA`, category labels, Arrow/Home/End nav helpers, checklist) and wired into `LabsShell` — skip link to `#labs-main`, `aria-pressed` category filters, Esc closes mobile drawer + focus restore, closed drawer `inert`/`aria-hidden`, cobalt `:focus-visible` rings. Mirrors Free Studying `freeStudyA11y`. No commit.
- **Fix Free Studying / STEM Labs new-tab (unconditional):** Removed pathname-gated `newTabLinkProps`. Marked Free Studying + STEM Labs `external: true` in `navConfig`; sidebar/mobile use plain `<a target="_blank" rel="noopener noreferrer">`. Home CTAs (Free Studying + STEM Labs) + FreeStudyLanding “Open STEM Labs” same; TopBar account Free Studying always `window.open(..., "_blank", "noopener,noreferrer")`. Hub/lab deep links stay same-tab. Files: navRoutes, navConfig, DashboardSidebar, MobileTabBar, DashboardTopBar, dashboard/page, FreeStudyLanding, skills.md. No commit.
- **BoardChatDock chrome polish:** Unified mic+field+send into one composer well; `BoardVoiceControls` `compact` mode (icon mic, no rail/status); aligned 2.5rem hit targets, cobalt focus rings, collapse/tab polish in `board-chat.module.css` + `board-voice.module.css`. No commit.
- **Fix free-study/labs hide primary dashboard chrome:** Hardened `shellRoutes.ts` (`normalizeDashboardPath`, `isFreeStudyFullscreenRoute`, `isLabsFullscreenRoute` incl. `/labs/[simId]`) into `isExamFullscreenRoute`. `DashboardShell` explicitly ORs those helpers + whiteboard; fullscreen branch never mounts `DashboardSidebar`/`DashboardTopBar`/`MobileTabBar` (`data-dashboard-chrome="hidden"`). Left `FreeStudyShell` / `LabsShell` own sidebars intact. Routes that hide primary chrome: `/dashboard/whiteboard`, `/dashboard/free-study`, `/dashboard/labs`, `/dashboard/labs/[simId]` (+ practice sessions / exams / question-rush as before). No commit.
- **LabsShell (STEM Labs chrome):** Fullscreen own layout for `/dashboard/labs` — `LabsShell` + `labs-shell.module.css` with cobalt Scholaris sidebar (categories All/Physics/Chem/Bio/Math, lab list, Dashboard back, Browse all). Wired `src/app/dashboard/labs/layout.tsx`; main dashboard chrome hidden via existing `isLabsFullscreenRoute`. Trimmed duplicate back links from `SimsCatalog` / `[simId]` page. Uses `--shell-*` tokens. Sims implementations untouched. No commit.
- **Own-shell new-tab research (Free Studying + STEM Labs):** Wrote `docs/research/free-study/10-own-shell-new-tab.md` only (no app code). Documented Whiteboard fullscreen via `shellRoutes` → `DashboardShell` `examLayout`; noted hide-chrome + new-tab helpers already landing; remaining focus = cobalt **product rail** (Notion/Figma/Excalidraw zen steal/skip); component map for hub/labs shell agents.
- **Free Studying UI audit (focused):** Cobalt-only PdfIntent tiles (dropped teal/orange); slim Voice Listen/Review/Ask underline (not fat step boxes); quieter Destinations back-link + Preload voice CTA; PDF/voice user copy (no telemetry jargon); chat pane height; mobile rail nowrap scroll; studio `skipLink`; restored Hub icon imports. No commit.
- **Apply `free_study` CHECK migration (MCP):** **NO** — DDL blocked. `apply_migration` → `Cannot apply migration in read-only mode.`; `execute_sql` → `ERROR: 25006: cannot execute ALTER TABLE in a read-only transaction`. Clients stay on `context_type: "general"` (`FreeStudyHub`, `useBoardTutor`). Migration file `20260720190000_tutor_conversations_free_study_context.sql` unchanged; apply via writable Supabase dashboard/CLI when available. No commit.
- **Shared studio shell tokens:** Added `src/styles/studio-tokens.css` (`--shell-*` cobalt/ink/paper/edge/glow/ease/chrome) imported from `globals.css`. Aliased Free Studying studio/landing + quiz/flashcards/notes/voice, Whiteboard shell chrome + atmosphere glints, STEM Labs `SimShell`/`sims-hub` to shared tokens. Kept WB desk `--studio-*` local to atmosphere. No commit.
- **Verify `tutor_conversations?select=id` no longer 400:** **PASS** (app path). Live REST (service_role): `GET …/tutor_conversations?select=id` → **200**; `POST …?select=id` + `context_type=general` → **201**; `free_study` still **400**/`23514` (CHECK not yet widened; migration `20260720190000` unapplied). Clients already use `general`. No commit.
- **Free Studying / STEM Labs new-tab CTAs:** Dashboard chrome opens Free Studying + STEM Labs in a new tab (`target="_blank"` + `rel="noopener noreferrer"`) via `isNewTabDashboardEntry` / `newTabLinkProps` in `navRoutes.ts`. Wired: sidebar Free Studying + STEM Labs, mobile Study + Labs tabs, home Free Studying CTA, account menu Free Studying, Free Studying landing “Open STEM Labs”. When already on `/dashboard/free-study` or `/dashboard/labs`, chrome links stay in-app so deep links (`?dest=`, hub rail, lab cards) keep working. No home STEM Labs CTA (none existed). No commit.
- **Verify CSP vercel.live + Perplexity font:** **PASS.** Confirmed `next.config.ts` allowlists `https://vercel.live` (+ Pusher/`assets.vercel.com`/`vercel.com`) for Toolbar; Perplexity CDN absent from app + excluded from `font-src` (extension noise). No commit.
- **Remove whiteboard PDF peek:** Deleted `BoardPdfPeek.tsx` + `board-pdf-peek.module.css`; unwired from `WhiteboardStudio` (no `pdfSlot` / `pdfExcerpt` state); dropped `pdfExcerpt` from `BoardChatDock` + `getPdfExcerpt` from `useBoardTutor`; cleaned `BOARD_ARIA.pdfPeek` / a11y notes / `.pdfSlot` CSS. Kept Free Studying `FreeStudyPdfPane` extract → intent → text-to-Mistral. No commit.
- **CSP console errors (vercel.live + Perplexity fonts):** Allowed Vercel Live/Toolbar via official CSP origins in `next.config.ts` (`script-src`/`connect-src`/`frame-src`/`img-src`/`style-src`/`font-src` for `https://vercel.live` + `wss://ws-us3.pusher.com` + `vercel.com`/`assets.vercel.com` as needed). Searched Free Studying + app for `frontend-cdn.perplexity.ai` — **no app loads**; did **not** add Perplexity to `font-src` (likely browser extension). No commit.
- **Verify whiteboard OCR/ask text telemetry + PDF peek removed:** **PASS.** Client PNG snapshot → Tesseract (`boardOcr`/`BoardAskBar`) stays in-browser; Ask sends string `ocrText` → `useBoardTutor` telemetry `{ source:"whiteboard", ocr_text }` via `useTutorStream` JSON; tutor Zod has string-only telemetry fields (no image/blob keys). Working tree deletes `BoardPdfPeek` + CSS; Studio/Dock/`index` no longer wire `pdfExcerpt`. Residual unused `getPdfExcerpt` on `useBoardTutor` only. No commit.
- **Verify FS + STEM Labs `target=_blank` nav/CTAs:** **FAIL overall** — no Free Studying / STEM Labs dashboard entry opens a new tab. FAILs: sidebar Free Studying + STEM Labs (`DashboardSidebar` plain `<Link>`); mobile Study + Labs tabs (`MobileTabBar`); home Free Studying CTA (`dashboard/page.tsx`); account menu Free Studying (`DashboardTopBar` `router.push`); Free Studying landing “Open STEM Labs” (`FreeStudyLanding`). ABSENT: home STEM Labs CTA. In-app exits/deep links (Whiteboard Close, `?dest=`, lab cards) correctly same-tab. Only Discord/Instagram More links use `_blank`. No commit.
- **tutor_conversations 400 fix:** Root cause — Free Studying / Whiteboard inserted `context_type: "free_study"`, but DB CHECK `tutor_conversations_context_type_check` only allows `exam_prep|lesson|question|general`. PostgREST surfaces that as `POST …/tutor_conversations?select=id` → **400** (not a bad `select=id`). Fix: clients use `context_type: "general"` in `FreeStudyHub.tsx` + `useBoardTutor.ts`. Added migration `20260720190000_tutor_conversations_free_study_context.sql` to allow `free_study` later (MCP apply/execute DDL is read-only). Typed `createTutorConversation` to include `free_study`. No commit.
- **FS Notes static path test (PASS):** Grepped `FreeStudyNotesPane` / hub `onAskScho` / telemetry. Flow attach→`runOcr`/`recognizeImageText`→editable OCR textarea→Ask with `{ source:"notes", note_excerpt?, ocr_text? }` only. Hub strips `pdf_excerpt`/`transcript` on notes; tutor Zod has no image fields; `useTutorStream` JSON-only. Image bytes only to optional R2 `/api/notes/images`. Gaps: OCR not persisted on note switch; OCR duplicated in chat message + telemetry; no automated test. No commit.
- **Verify Voice pane STT→transcript→Mistral (no audio bytes):** **PASS.** STT = Web Speech API (`createSttSession` / `useLocalTelemetryModels`) → string only. `FreeStudyVoicePane` Ask → `{ message, transcript }`; hub `askScho(..., { source: "voice", transcript })`. Tutor Zod accepts `transcript: string` only; `formatTelemetryPayload` → `Voice transcript:\n…` system text. No MediaRecorder / audio blob / base64 to Mistral. Kokoro PCM is client playback only. No commit.
- **Verify `/api/ai/free-study/flashcards` text-only:** **PASS.** Route `flashcards/route.ts`: JSON-only, `rejectBlobFields` + `FLASHCARDS_FORBIDDEN_BLOB_KEYS` → 400 `TEXT_ONLY` for file/blob/image/pdf/base64 keys; Zod `.strict()` `{ text, exam_type?, count? }` (min 40 chars). Client `generateFlashcardsFromText` POSTs text only. No commit.
- **Verify PDF upload→extract→PdfIntentChooser→quiz/flashcards/ask/summarize text-only:** **PASS.** Chain: `FreeStudyPdfPane` upload → `extractPdfTextClient` (pdf.js, File stays in browser) → excerpt → `PdfIntentChooser` → hub `onPdfIntent`: ask/summarize via `askScho` + `pdf_excerpt` telemetry; quiz via `generateQuiz({ text })` → `FreeStudyQuizPanel`; flashcards via `FreeStudyFlashcards` → `generateFlashcardsFromText`. APIs reject blob keys (`TEXT_ONLY`). No PDF bytes to Mistral. No commit.
- **Dashboard fullscreen chrome (free-study + labs):** Extended whiteboard pattern in `shellRoutes.ts` — added `isFreeStudyFullscreenRoute` + `isLabsFullscreenRoute`; wired into `isExamFullscreenRoute` so `DashboardShell` / `MobileTabBar` hide primary sidebar, top bar, and tab bar on `/dashboard/free-study` and `/dashboard/labs` (same `examLayout` / `data-shell=fullscreen` as whiteboard). Comments in `DashboardShell` + `dashboard/layout.tsx` updated. Left FreeStudyHub/Labs page shells untouched for sibling agents. No commit.
- **Verify free-study/labs hide sidebar like whiteboard:** **PASS** (fixed same session via helpers above). No commit.
- **Verify `/api/ai/free-study/quiz` text-only:** **PASS.** Route `quiz/route.ts`: JSON-only (`req.json`), Zod `.strict()` `{ text, exam_type?, count? }`, `rejectBlobFields` → 400 `TEXT_ONLY` for file/blob/image/pdf/base64 keys; prompt uses string text only via `completeJson`. Client `generateQuiz.ts` refuses non-string `text`, POSTs `application/json`. Hub quiz intent passes `clipped` excerpt only. No commit.
- **GitHub commit + push (FreeStudyHub notes/flashcards):** Committed pending `FreeStudyHub.tsx` — notes Ask Scho uses `ocr_text`/`note_excerpt` only (never image bytes); notes rail meta “OCR”; flashcards artifact `nonce` + remount key so re-clicks regenerate. Pushed `master` → `origin/master`. No secrets. No force-push.
- **Whiteboard Studio UX polish:** Centered Scho open ceremony (`BoardSchoOpen` + `board-scho-open.module.css`) in true stage middle (not dock-aligned); dock waits via `revealReady`. Redesigned `BoardChatDock` + `board-chat.module.css` — display Scho header, message bubbles, composer well, empty state, collapse pill. Fixed orphan CSS in `pdf-intent.module.css` blocking build. `npm run build` exit 0; commit+push `origin/master`.
- **Notes pane OCR→Ask:** Polished `FreeStudyNotesPane` + `free-study-notes.module.css` — attach image → client Tesseract OCR → editable text → Ask Scho with telemetry `ocr_text`/`note_excerpt` (never image bytes to Mistral). Best-effort R2 when note saved; OCR works without R2. Wired into redesigned `FreeStudyHub` notes destination.
- **FreeStudyPdfPane (text-only PDF path):** `FreeStudyPdfPane.tsx` + `free-study-pdf.module.css` — upload → `extractPdfTextClient` (pdf.js) → excerpt → `PdfIntentChooser` → hub `onIntent` (ask/summarize via `useTutorStream` + `pdf_excerpt`; quiz via `generateQuiz` text-only; flashcards). Never sends PDF bytes to Mistral.
- **Free Studying hub polish (this agent):** Followed `00-CONTRACT.md`. Confirmed no fat mode-pill strip; destination IA via `FreeStudyLanding` + slim hub rail. Wired/aligned siblings (`FreeStudyPdfPane`, quiz-primary `PdfIntentChooser` + `tilePrimary`, `FreeStudyImageAsk`, Voice/Notes panes, quiz/flashcards artifacts). Deep links `?dest=` (and legacy `?mode=`); Whiteboard → `/dashboard/whiteboard`. Cobalt studio CSS. Hub null-mode falls back to `FreeStudyLanding`.
- **FS flashcards/summarize/voice/notes verify:** CONTRACT + hub wiring confirmed — flashcards (`generateFlashcards` + API + `FreeStudyFlashcards`) on PdfIntentChooser; summarize via `summarizeIntent` telemetry; Voice/Notes panes pass text-only `transcript`/`ocr_text`/`note_excerpt`. Hub flashcards remount `nonce` on re-click. Text only to Mistral. No commit.
- **GitHub commit + push (Free Studying redesign):** Committed `6cee663f` — `feat: redesign Free Studying with PDF/image text→Mistral quiz flow`. Pushed `master` → `origin/master` (`d8a36889..6cee663f`). Excluded `.env.local`. No force-push. Remote: https://github.com/Yatishgrandhe/scholaris.git.
- **Free Studying INTEGRATOR delivery:** Wired `FreeStudyHub` to destination rail + `FreeStudyPdfPane` / `PdfIntentChooser` / `FreeStudyQuizPanel` / `FreeStudyFlashcards` / `FreeStudyImageAsk` / `FreeStudyVoicePane` / `FreeStudyNotesPane`; page landing via `FreeStudyLanding` (`?mode=` deep links). PDF/image → extract text only → Mistral (`pdf_excerpt` / `ocr_text` / quiz+flashcards APIs; never file bytes). `npm run build` exit 0. Routes: `/dashboard/free-study`, `/api/ai/free-study/quiz`, `/api/ai/free-study/flashcards`. Research `docs/research/free-study/`. Commit `6cee663f` on `origin/master` (no secrets / no force-push).
- **Free Studying Voice pane polish:** Clearer STT→Review→Ask Scho flow in `FreeStudyVoicePane.tsx` + cobalt `free-study-voice.module.css` (not whiteboard). Hub Voice mode embeds pane; Ask Scho passes `{ source: "voice", transcript }` so STT text reaches Mistral as multimodal text telemetry via `formatTelemetryPayload`. Optional focus field; stop mic on ask. No commit.
- **FS summarize intent:** Helper `summarizeIntent.ts` (`buildSummarizeTutorTurn` / `SUMMARIZE_USER_MESSAGE`) — extracted PDF/OCR text only in telemetry (`pdf_excerpt`/`ocr_text` + `intent: "summarize"` system framing); short user message (no file bytes). PdfIntentChooser Summarize chip → `buildSummarizeFromExcerpt`; hub `onPdfIntent` → `askScho(message, telemetry)`. Tutor route + `telemetryPayload` accept/format intent. No commit.
- **FS flashcards from text:** Text-only Mistral flashcards from extracted PDF/OCR — `generateFlashcards.ts` (prompt/normalize + `aiRequest` client), `POST /api/ai/free-study/flashcards` (auth/CSRF/rate-limit/keyPolicy; rejects blob keys), `FreeStudyFlashcards.tsx` + cobalt CSS (flip deck, regenerate, list). Integrator wires PdfIntentChooser `flashcards` intent. No commit.
- **Free Studying redesign CONTRACT:** Wrote `docs/research/free-study/00-CONTRACT.md` only (no app code). Master inspiration list of **58** named products (sibling tracks 01–09 + whiteboard extras); frozen IA — no fat top mode-tab strip; destinations Tutor / Whiteboard Studio (`/dashboard/whiteboard`) / PDF / Voice / Notes; PDF/image → extract text → telemetry to Mistral (never binaries); post-upload intent chooser with **Generate quiz** primary; cobalt Scholaris CSS direction; component file map for build agents.
- **Mobile study apps research:** Wrote `docs/research/free-study/09-mobile-study.md` only (no app code). Khan Academy next-action/resume; Forest/Focus single Plant CTA + costly Give Up; Goodnotes iOS vs Web honesty (parity/sync/Cloud); Notability ink+audio/Tape/auto-deselect eraser; Concepts movable Tool Wheel + chrome ladder; thumb Easy/Stretch/Hard + single primary CTA; Scholaris Free Studying ≤768px checklist.
- **FreeStudyQuizPanel:** Interactive check-yourself UI for `generateQuiz` JSON (`GenerateQuizResult` / `GenerateQuizQuestion[]`) — select options, Check answers, score + explanations, Try again. Cobalt CSS in `free-study-quiz.module.css`. Files: `FreeStudyQuizPanel.tsx`, `free-study-quiz.module.css`.
- **PDF+ask / quiz research (Free Studying):** Wrote `docs/research/free-study/03-pdf-ask-quiz.md` only (no app code). Compared Kami, Acrobat web + Student Spaces, Hypothesis, LiquidText, ChatPDF, Humata, NotebookLM/Gemini Notebook on upload → extract → intent picker (quiz/summarize/tutor/flashcards). Verdict: ship hard Studio grid + selection-scoped verbs; chat = Tutor lane with soft suggested Qs.
- **Photo-math ask UX research:** Wrote `docs/research/free-study/05-photo-math-ask.md` only (no app code). Photomath / Wolfram|Alpha / Symbolab / Socratic (legacy) / Gauth — camera→isolate→editable OCR→gated steps/explain; Scholaris mapping = crop + edit transcript + Ask Scho.
- **Voice+study research:** Wrote `docs/research/free-study/08-voice-study.md` only (no app code). Compared ChatGPT Voice/GPT-Live, Gemini Live, ELSA Speak-like, Speechify, NaturalReader. Verdict: document-primary + synced listen (Speechify/NR) + barge-in manners (GPT/Gemini); ELSA scoring opt-in only; avoid fullscreen voice orb dominating PDF.
- **Edu platforms research (free-study):** Wrote `docs/research/free-study/02-edu-platforms.md` only (no app code). Khan / Brilliant / Quizlet / AnkiWeb / Duolingo — study-session UX + practice-from-content (bank+selector vs source→artifacts); Scholaris Free Studying steal/anti-pattern map.
- **Free Studying landing first-paint:** Brand-forward cobalt home at `/dashboard/free-study` (no mode pill strip). `FreeStudyLanding.tsx` + `free-study-landing.module.css` — Scholaris / Free Studying display hero, destination rail to Tutor·Whiteboard·PDF·Voice·Notes (Whiteboard → `/dashboard/whiteboard`; others `?mode=`). Page shows landing unless `mode` set → `FreeStudyHub`. STEM Labs footer link. Cool cobalt atmosphere (not cream/purple).
- **Free Studying image OCR ask:** `FreeStudyImageAsk.tsx` + `free-study-image-ask.module.css` — upload photo → Tesseract via `recognizeImageText` (`ocr.ts`) → editable text → Ask Scho with telemetry `ocr_text` only (never image bytes to Mistral). Wired into hub Tutor + Notes panes; `buildTelemetry` includes `ocr_text`; prompt label generalized to “OCR text”. R2 note “Attach image” unchanged (storage). No commit.
- **Product chrome research (Free Studying):** Wrote `docs/research/free-study/06-product-chrome.md` only (no app code). Linear/Arc/Framer/Figma/Vercel/Stripe/Raycast — typography 13–14px, density vs visual noise, nav without mode-pill racks; cobalt anti-purple checklist for hub shell.
- **Free-study quiz from text (BYOK):** `POST /api/ai/free-study/quiz` accepts `{ text, exam_type?, count? }` only (strict Zod + reject file/blob keys; never blobs). CSRF + auth + `generate-question` rate limit + `resolveAiClient` (same keyPolicy as tutor) → `completeJson` MCQs. Client: `src/lib/free-study/generateQuiz.ts` via `aiRequest`. Files: `src/app/api/ai/free-study/quiz/route.ts`, `src/lib/free-study/generateQuiz.ts`.
- **Notes shells research (Free Studying):** Wrote `docs/research/free-study/04-notes-shells.md` only (no app code). Compared Notion, Obsidian app+Publish, Apple Notes web analogs (iA/Bear/Calmly-class), OneNote+Copilot, Craft, Reflect.app, Mem.ai for calm writing surfaces and image attach→AI. Verdict: Craft/iA calm + Reflect OCR→search/chat + OneNote page-grounded Copilot; hard-no Mem blind attachments. Scholaris recipe: readable column, attach→editable OCR→Ask Scho dock.
- **AI chat shells research (Free Studying):** Wrote `docs/research/free-study/01-ai-chat-shells.md` only (no app code). Compared ChatGPT / Claude.ai / Perplexity / Gemini / Poe: shell layout, mode switching without fat top tabs, artifact+chat. Verdict: composer tools + context chips + optional side artifact; kill Tutor/PDF/Voice/Notes peer tabs; keep Whiteboard/Labs as routes. Steal/skip lists for hub redesign.
- **PdfIntentChooser:** After PDF extract, beautiful intent UI (Ask Scho / Generate quiz / Summarize / Flashcards) in `PdfIntentChooser.tsx` + `pdf-intent.module.css`; emits `onIntent(intent, excerpt)`; light FreeStudyHub PDF-pane wire only (no hub rewrite).
- **BoardChatDock (finish):** Glass/rail AI dock — `useBoardTutor` (OCR+transcript+pdf telemetry), `FreeStudySectionedReply`, `BoardVoiceControls` mic → composer, Kokoro play, `useBoardMotion` slide, `boardA11y` chat log. Collapsible side/bottom overlay; handle `ask`/`expand`/`collapse`/`setOcrText`. Files: `BoardChatDock.tsx`, `board-chat.module.css`; small `askScho` options on `useBoardTutor`. Agent finished after integrator; source already in `4e8a9897` — no extra commit.
- **BoardChatDock post-integrator check:** Working tree had no uncommitted whiteboard/chat source — `BoardChatDock.tsx` / `board-chat.module.css` / `useBoardTutor.ts` already in `4e8a9897` (HEAD `d8a36889` = docs log after that). Branch up to date with `origin/master`. No build/commit/push; no empty commit.
- **GitHub commit + push (Whiteboard Studio):** Committed `4e8a9897` — `feat: ship immersive Whiteboard Studio at /dashboard/whiteboard`. Pushed `master` → `origin/master` (`4b1bbafb..4e8a9897`). Excluded `.env.local`. No force-push. Remote: https://github.com/Yatishgrandhe/scholaris.git.
- **Whiteboard Studio INTEGRATOR delivery:** Wired `WhiteboardStudio` + `index.ts` over sibling Board* primitives; route `/dashboard/whiteboard` prerenders (Next build lists it). Hub Whiteboard → `router.push` full-page (no in-hub mode tabs). Shell chrome hides via `isWhiteboardFullscreenRoute`. Chat dock telemetry: OCR + optional PDF peek. Research under `docs/research/whiteboard/` (CONTRACT + 01–10). Polish: Stitch-like full-bleed, floating tools, Ask strip, AI dock with voice/TTS. `npm run build` exit 0. Whiteboard TS clean (`tsc` pre-existing non-whiteboard noise only). Commit+push `origin/master` (exclude `.env.local`; no force-push).
- **WhiteboardStudio shell:** Replaced stub with full-page Stitch-like composition at `src/components/whiteboard/WhiteboardStudio.tsx` + `whiteboard.module.css` + `index.ts`. Wires BoardAtmosphere → BoardCanvas (primary) + BoardEmptyHint + BoardTopEsc + BoardToolbar + BoardAskBar + BoardPdfPeek + BoardChatDock (`ocrText`/`pdfExcerpt`; dock owns useBoardTutor + BoardVoiceControls + Kokoro). Hotkeys via `useBoardHotkeys`; stroke flash via `useBoardMotion`. Sibling CSS modules imported. No FreeStudyHub edits.
- **Whiteboard Studio nav entry:** Distinct open path for `/dashboard/whiteboard` without cluttering STEM Labs. Free Studying sidebar **child** “Whiteboard Studio”; More **Study** section (mobile); account menu shortcut. Constants `WHITEBOARD_HREF`/`WHITEBOARD_NAV_ID` in `navRoutes`; `NavItem.children` + `WHITEBOARD_NAV_ITEM` in `navConfig`. STEM Labs unchanged. Files: `navRoutes.ts`, `navConfig.ts`, `DashboardSidebar.tsx`(+css), `DashboardTopBar.tsx`, `MobileTabBar.tsx`.
- **BoardCanvas (ink primitive):** `src/components/whiteboard/BoardCanvas.tsx` + `board-canvas.module.css`. Pointer Events + `touch-action:none` + capture; coalesced points + `strokeMath` pressure/RDP+Chaikin commit; ResizeObserver DPR; transparent ink over cool fiber/ruled/dot paper (not flat white); `BoardCanvasHandle` undo/clear/`exportPng`|`snapshotBlob` + `onInkChange`/`onUndoAvailabilityChange`/`onStrokeEnd`. Export composites paper+ink PNG for `boardOcr`. Did not touch Studio/Toolbar/Chat. No commit.
- **Whiteboard OCR ask bridge:** `boardOcr.ts` snapshots canvas/PNG → reuses free-study `recognizeImageText` as `ocrImage` (PSM sparse handwriting) → clear typed errors; `ocrFromBoardHandle` for BoardCanvas; `BoardAskBar` + `board-ask.module.css` = Read handwriting → edit → Ask Scho chat prompt. No Free Study CSS edits.
- **Whiteboard Studio a11y:** Owned `boardA11y.ts` + `BoardA11yNotes.md` — shared aria labels, live-region helpers (OCR polite / errors assertive / chat log), tool button label helpers, `BOARD_A11Y_CHECKLIST` for toolbar/canvas/dock consumers.
- **BoardVoiceControls:** Whiteboard Studio mic rail — toggle + push-to-talk via `createSttSession` / `isSpeechRecognitionSupported` from `@/lib/free-study/stt` (no STT rewrite); `onTranscript` + `onSessionEnd` for chat dock; cobalt glass CSS in `board-voice.module.css`. Files: `src/components/whiteboard/{BoardVoiceControls.tsx,board-voice.module.css}`.
- **Khan tutoring whiteboard research:** Wrote `docs/research/whiteboard/07-khan-tutoring.md` only (no app code). Covers Khan scratchpad vs GPT-4o/Khanmigo multimodal session patterns; draw+Socratic chat layouts; look-on-send vs continuous vision; session state machine; anti-patterns; Scholaris mapping (explicit Ask tutor).
- **BoardToolbar (floating tools):** Shipped `src/components/whiteboard/BoardToolbar.tsx` + `board-toolbar.module.css` — Stitch-like bottom-center cluster (not fat top bar): pen/eraser, 5 ink swatches, 3 widths, undo/clear; frosted cobalt chrome; peek pill + auto-collapse (~2.4s idle, pin on peek click, Escape); `forceExpanded` for drawing; exports `BOARD_INK_COLORS` / `BOARD_STROKE_WIDTHS`. Aligns with `useBoardMotion` toolbar timing tokens. No Free Study edits. No commit.
- **Whiteboard Studio PDF peek:** Shipped `BoardPdfPeek.tsx` + `board-pdf-peek.module.css` — optional floating paste/upload excerpt rail (client pdf.js, 6k clip for `pdf_excerpt` telemetry); not Kami. Studio wires `onChange`. No Free Study edits.
- **tldraw UX research:** Wrote `docs/research/whiteboard/03-tldraw.md` only (no app code). Covered pointer model (InputsManager screen/page/origin, fine vs coarse drag thresholds, StateNode tools, pen mode), floating UI slots/contextual toolbars/InFrontOfTheCanvas/focus mode, mobile breakpoints + coarse pointer + keyboard-safe toolbar, AI kits (Agent/Chat/Branching/Workflow + dual screenshot+structured export). Sources: tldraw.dev docs/starter kits.
- **Whiteboard full-bleed chrome:** `/dashboard/whiteboard` hides sidebar + top bar + mobile tab bar via `isWhiteboardFullscreenRoute` → `isExamFullscreenRoute` (`shellRoutes.ts`); `DashboardShell` uses existing `examLayout` (`data-shell=fullscreen`). Other dashboard pages unchanged. `BoardTopEsc` remains the exit.
- **Whiteboard `useBoardHotkeys`:** `src/components/whiteboard/useBoardHotkeys.ts` — V/P pen, E eraser, Z (and ⌘/Ctrl+Z) undo, `[`/`]` width steps, Esc blur, Space+drag pan via `spaceHeld`/`onSpaceHeldChange` (`enableSpacePan`); skips editables; shortcut table in file header.
- **Miro/FigJam education UX research:** Wrote `docs/research/whiteboard/06-miro-figjam.md` only (no app code). Verdict: steal focus/structure/ink/snapshot jobs; skip multiplayer facilitation, sticky-as-culture, Present/slides, stamps/widgets. Framed for Scholaris 1:1 tutoring + OCR→Ask Scho (not Miro clone).
- **PDF annotation UI research:** Wrote `docs/research/whiteboard/08-pdf-annotation.md` only (no app code). Kami / Acrobat web / Hypothesis / LiquidText (+ Flexcil/Shadow/Shelf). For Free Studying PDF ‖ board: selection-first Highlight·Send·Ask Scho; dual ink+clip list with jump-back; semantic colors; declutter; provenance; minimal on-page ink, synthesis on board; Ask on selection not whole file. Skip LMS/social/redline/canvas-as-PDF.
- **Excalidraw whiteboard UX research:** Wrote `docs/research/whiteboard/02-excalidraw.md` only (no app code). Covered tools/shortcuts, gestures (Space/pan, Z+drag zoom, pen-mode palm rejection, touch gaps), minimal UI (zen/view, contextual props, overflow extras), export (PNG/SVG/JSON/clipboard + embed scene), education patterns, and tutoring steal-list. Sources: README, docs, Plus education/how-to, DeepWiki toolbars, GitHub pen/touch PRs, MakeUseOf.
- **GoodNotes/Notability whiteboard UX research:** Wrote `docs/research/whiteboard/04-goodnotes-notability.md` only (no app code). Covered pen feel (latency/pressure/tilt/palm/gestures/Smart Ink vs audio-sync), tool chrome (presets, dockable strips, auto-deselect eraser, Pencil squeeze/double-tap), paper metaphor (notebooks/covers/templates vs subjects; page vs infinite Whiteboard+minimap; Tape). Web analogs: OneNote Draw, MyScript Notes/Nebo, Concepts, Excalidraw, Freeform/MS Whiteboard. Scholaris implications: stroke fidelity + touch policy + eraser return + quiet paper; avoid Miro chrome.
- **BoardTopEsc:** Minimal Stitch-like corner Close (X + Esc→`/dashboard/free-study`) in `BoardTopEsc.tsx` + module CSS — not a fat header; frosted pill, cobalt focus, safe-area.
- **AI canvas/chat research (whiteboard track):** Wrote `docs/research/whiteboard/09-ai-canvas-chat.md` only (no app code). Compared ChatGPT Canvas, Claude Artifacts, Gemini Canvas, Cursor IDE side-chat + Cursor Canvases. Verdict: durable primary artifact + conversational side channel; Scholaris should use artifact-center + Scho chat-right (Cursor-primary) with highlight-to-ask, versions, shortcut chips, preview/source from consumer canvases.
- **WB strokeMath:** Added `src/components/whiteboard/strokeMath.ts` — pure unit-free helpers for BoardCanvas: RDP `simplifyPath`, Chaikin `smoothPath` / `refineStrokePath`, `pressureToWidth` (+ defaults matching FreeStudyWhiteboard curve), `ROUND_STROKE_STYLE` / round cap·join constants, arc-length + pressure width samples. No DOM/React.
- **Whiteboard BoardAtmosphere:** Immersive drafting-desk shell — cool slate desk + warm tungsten lamp + aqua/cobalt cool fill; SVG paper grain + soft vignette; `--studio-*` CSS vars; no purple / no cream+terracotta. Files: `board-atmosphere.module.css` + thin `BoardAtmosphere.tsx` (`data-lamp` dim|bright).
- **Explain Everything whiteboard research:** Wrote `docs/research/whiteboard/05-explain-everything.md` only (no app code). Covers talk-while-draw recording patterns: one-button multimodal capture, object/event timeline vs pixel screencast, dual-track A/V + Mix/Overwrite/Insert, Camera Frame, slides-as-scenes, live voice chat vs narrated export; peer map (Educreations, ShowMe, Loom); Scholaris implications (P0 scrub replay vs OCR snapshot job split).
- **`useBoardTutor` hook:** Encapsulates Free Studying–style `ensureConversation` (`context_type: free_study`, title Whiteboard) + `useTutorStream` + telemetry `{ source: "whiteboard", ocr_text, transcript?, pdf_excerpt? }` for `BoardChatDock`. Sync OCR via `setOcrText` or live `getOcrText`/`getTranscript`/`getPdfExcerpt` getters. File: `src/hooks/useBoardTutor.ts`.
- **WB motion polish:** Shipped `board-motion.module.css` + `useBoardMotion.ts` — CSS-only dock slide, toolbar fade, stroke-commit flash; `prefers-reduced-motion` gated; no animation libs.
- **Google Stitch UX research (whiteboard):** Wrote `docs/research/whiteboard/01-google-stitch.md` only (no app code). Findings: Stitch Mar 2026 canvas-first shell — full-bleed infinite canvas; minimal edge chrome; bottom intent dock (text/attach/App↔Web/model/Live Mode); voice deixis + live frame updates; Agent Manager as spatial nav; Gemini Canvas / AI Studio Build as artifact-primary + docked chat parallels. Portable checklist for tutoring boards.
- **WB empty onboarding:** `BoardEmptyHint.tsx` + `board-empty.module.css` — first-paint “Write anything — then talk to Scho” (DM Serif italic + cobalt Scho), dismiss via `hasStroke`; pointer-events none; reduced-motion fade.
- **Whiteboard research (Concepts/Freeform):** Wrote `docs/research/whiteboard/10-concepts-freeform.md` only—no app code. Covers Concepts infinite sketch (vector canvas, finger=pan/stylus=ink, Tool Wheel→compact→hidden chrome ladder, HUD/status strip, lost-canvas recovery), Apple Freeform-lite (full screen + hide sidebar, fit/selection zoom, calm object+draw thinness), immersive full-page hide-chrome patterns, and Scholaris recipe: Concepts immersion + Freeform-lite tools → OCR→Ask tutor (not Miro).
- **Whiteboard Studio route shell:** Added `docs/research/whiteboard/CONTRACT.md` (immersive Stitch-like `/dashboard/whiteboard`, no Free Studying mode tabs, canvas-primary + floating tools + OCR ask + AI chat dock; component file map for other agents) + `src/app/dashboard/whiteboard/{page,layout}.tsx` importing stub `@/components/whiteboard/WhiteboardStudio`. Did not edit FreeStudyHub / free-study CSS.
- **Free Studying hub Whiteboard entry:** Hub modes are Tutor/PDF/Voice/Notes only; Whiteboard button `router.push('/dashboard/whiteboard')` (no in-hub whiteboard pane).
- **Free Studying whiteboard fix:** Research (Excalidraw/tldraw/tutoring boards): Pointer Events + `touch-action:none` + capture; pen/eraser via `destination-out`; undo via stroke snapshots; PNG snapshot → OCR → editable text → Ask tutor (not a Miro clone). Shipped `FreeStudyWhiteboard.tsx`: mouse/touch draw, pen/eraser/colors/widths/undo/clear, ResizeObserver DPR canvas, coalesced strokes + pressure fallback, empty state, Read handwriting + Ask Scho (auto-OCR if needed), clear OCR errors; hub uses sync OCR ref for telemetry; `ocr.ts` PSM sparse text. Cobalt OnePrep-ish chrome in `free-study.module.css`. `npm run build` exit 0. No commit.
- **Kokoro TTS ORT WASM fix:** Root cause — `@huggingface/transformers` defaults `env.backends.onnx.wasm.wasmPaths` to `cdn.jsdelivr.net`, which CSP blocks → `Failed to fetch … ort-wasm-simd-threaded.jsep.mjs` / “no available backend”. Fix: `scripts/copy-ort-wasm.mjs` → `public/ort/` (gitignored); `configureKokoroOrt` sets `wasmPaths=/ort/` + `numThreads=1` before Kokoro; worker + main-thread; clear dual-backend UI error; postinstall/predev/prebuild. Text-only tutor unchanged. `npm run build` exit 0. No commit (not requested).
- **GitHub commit + push (STEM Labs route):** Committed `5070571e` — `feat: give STEM Labs its own dashboard route and nav`. Pushed `master` → `origin/master` (`1aaef4d1..5070571e`). Routes `/dashboard/labs`, `/dashboard/labs/[simId]`; redirects from `/dashboard/free-study/sims/**`; nav + mobile Labs tab; Free Studying Sims demoted. Excluded `.env.local`. Remote: https://github.com/Yatishgrandhe/scholaris.git.
- **STEM Labs own route + nav:** Separated Science/STEM Labs from Free Studying. New routes `/dashboard/labs` + `/dashboard/labs/[simId]` (6 physics SSG packs preserved). Primary sidebar **STEM Labs** + mobile tab **Labs**; Free Studying Sims mode removed (light “Open STEM Labs” link kept). Permanent redirects from `/dashboard/free-study/sims/**`. Split `navRoutes.ts` so dashboard RSC doesn’t import Phosphor client icons (fixes `/dashboard` createContext build failure). `npm run build` exit 0.
- **GitHub commit + push (Free Studying nav):** Committed `b1499337` — `fix: promote Free Studying in dashboard nav`. Pushed `master` → `origin/master` (`326dc2c6..b1499337`). Files: `navConfig.ts`, `DashboardSidebar.tsx`, `MobileTabBar.tsx`, `DashboardTopBar.tsx`, `dashboard/page.tsx`, `dashboard.module.css`, `src/types/supabase.ts` (−2898 stale backup types), `skills.md`. Excluded `.env.local` (gitignored). Remote: https://github.com/Yatishgrandhe/scholaris.git.
- **Free Studying sidebar visibility:** Root cause — Free Studying was in `navConfig` main nav but easy to miss (placed after Scho + duplicated only in More; sidebar `display:none` ≤768px with no primary mobile tab; dashboard home had no CTA). Fix: promote to #2 primary sidebar item with **New** badge; remove More “Study” section; mobile tab **Study** → `/dashboard/free-study`; home hero **Free Studying** CTA; sidebar active/`data-nav-id` harden; account menu uses `FREE_STUDY_HREF`. Files: `navConfig.ts`, `DashboardSidebar.tsx`, `MobileTabBar.tsx`, `DashboardTopBar.tsx`, `dashboard/page.tsx`, `dashboard.module.css`. Verified: `getNavForExam` main order `home > free-study > scho > …`.
- **Agent 2 DB shrink (VACUUM):** `questions` before/after identical — total **341 MB** (heap 251 / toast 61 / indexes 29); DB **365 MB**. `VACUUM (ANALYZE, VERBOSE)` on `questions` (+ toast `pg_toast_18572`), `vocabulary_words`, `exam_sessions`, `question_attempts`, `tutor_messages` via pooler :5432 + `pg`; 0 dead removable, 0 pages removed; no `VACUUM FULL`; no mass deletes; backup `questions_expmodel_backup_20260630` already absent (`to_regclass` null).
- **Agent 3 orphan/dead schema:** Live public inventory = 45 tables, **0** `*backup*`/`temp` relations left (prior drops in `20260719020000` + `20260720160000`). No safe DROP this pass. Kept all core/zero-row tables (enrollments, practice_*, passages, etc. — app-referenced). Pruned **23** stale backup type blocks from `src/types/supabase.ts` (−2898 lines). `VACUUM ANALYZE` via MCP; DB still **~365 MB** (bloat is `questions`, not orphans). Deferred: unused-looking empty tables that are still in app code; advisor RLS/SECURITY DEFINER warnings.
- **Change summary (git facts):** Summarized Free Studying multimodal + STEM Lab delivery on `master` @ `326dc2c6` = `origin/master` (https://github.com/Yatishgrandhe/scholaris). Feature commit `b6c33726` (~7.3k LOC feature adds; overall ~4.9k files / −1.66M lines mostly pruned import artifacts). Areas: Free Study hub routes/UI; OCR/STT/Kokoro + CSP; notes+R2 APIs; 6 physics labs; DB migrations + repo cleanup; docs sync commits `7ebf582a`/`326dc2c6`.
- **GitHub push (recheck):** Working tree clean — no new feature commit. Pushed `master` → `origin/master` (`git push -u origin HEAD`); result **Everything up-to-date**. HEAD `7ebf582a` — docs: record GitHub sync of master to origin. Remote: https://github.com/Yatishgrandhe/scholaris.git.
- **GitHub sync:** Working tree was clean (no new commit). Set `origin` → `https://github.com/Yatishgrandhe/scholaris.git`. Local `master` (`b6c33726`) and remote `main` (`a10802bd`) have **unrelated histories** (no merge-base) — did **not** force-push `main`. Pushed `master` → `origin/master` with upstream set. PR link suggested by GitHub (not created): https://github.com/Yatishgrandhe/scholaris/pull/new/master. Existing remote `prep` → scholarisprep left intact.
- **Delivery complete:** Free Studying + STEM Lab gap audit done; removed dead `listFreeStudySimStubs`; verified routes (`/dashboard/free-study`, 6 SSG sims), tutor telemetry + sectioned replies, notes/R2 APIs, CSP/nav/`.env.example`; `npm run build` exit 0; committed (push skipped — no upstream on `master`). Human: set optional `R2_*` in `.env.local` for note images.
- `npm run build` green: fixed STEM lab RSC prerender (`SimLabHost` takes `simId`; catalog uses `SimManifestMeta` without `load`). Pruned shipped physics from Free Study planned stubs.
- Verified **collisions-1d** + **ucm-orbits** complete (model/register/Lab/Scene2D + index imports); Free Study planned id aligned to `collisions-1d`. No full build.
- Added `free_study_notes` + `free_study_note_assets` table types to `src/types/supabase.ts` (types agent hit usage limit; done inline).
- Physics P0 complete: forces-fbd, work-energy, collisions-1d, ucm-orbits (+ earlier projectile-2d, shm) — all six self-registered.
- Applied `20260720120000_free_study_notes.sql` remotely via Postgres (MCP apply_migration read-only); tables + 8 RLS policies live.
- Physics P0 labs **collisions-1d** + **ucm-orbits** under `src/sims/physics/**` only (predict→run→compare, Ideal/Dissipative collisions + UCM/gravity-orbit modes, Canvas2D + DOM, self-register); wired in `index.ts`. No full build.
- Physics P0 labs **forces-fbd** + **work-energy** under `src/sims/physics/**` (Ideal/Dissipative fidelity, predict→run→compare, Canvas2D + DOM, self-register via `register.ts` + `index.ts`); no next.config/package.json. No full build.
- **Free Studying multimodal core (complete):** hub panes + Kokoro/OCR/STT + CSP + sidebar/account + notes/R2/sharp + sectioned replies + Play with Kokoro. Skip full `npm run build`. Apply `supabase/migrations/20260720120000_free_study_notes.sql` to remote (MCP apply was read-only). Set optional `R2_*` in `.env.local` for note images.
  - **Try:** login → sidebar **Free Studying** or avatar menu → `/dashboard/free-study` → modes Tutor / Whiteboard (OCR→Ask) / PDF / Voice / Notes / Sims; Play with Kokoro on assistant replies; BYOK Mistral in Settings.
  - **Files:** `src/app/dashboard/free-study/page.tsx`, `src/components/free-study/{FreeStudyHub,FreeStudySectionedReply,free-study.module.css}`, `src/lib/free-study/{ocr,stt,speechText,simRegistry}.ts`, `src/lib/ai/telemetryPayload.ts`, `src/lib/aiPrompts.ts`, `src/lib/promptBuilder.ts`, `src/app/api/ai/tutor/route.ts`, `src/hooks/{useTutorStream,useKokoroTts,useLocalTelemetryModels}.ts`, `src/workers/kokoroTts.worker.ts`, `src/lib/storage/{r2,noteImages}.ts`, `src/app/api/notes/images/**`, `supabase/migrations/20260720120000_free_study_notes.sql`, `next.config.ts` (CSP), `src/lib/dashboard/navConfig.ts`, `src/components/layout/{DashboardTopBar,DashboardTopBar.module.css,DashboardSidebar.module.css}`, `src/components/shared/skeletons/skeletons.module.css`, `src/lib/rateLimit.ts`, `.env.example`, `package.json` (tesseract.js, kokoro-js, sharp, @aws-sdk/*). Sims routes also under `src/app/dashboard/free-study/sims/**` (STEM W0–W1).
- STEM Lab W0–W1 foundation shipped: frozen contracts (`SimManifest`, fidelity Ideal/Dissipative/Illustrative, `SimKernel`, `SimTelemetry`); shared `SimShell` + `VisualStage` tiers; catalog + routes `/dashboard/free-study/sims`; P0 Canvas2D **projectile-2d** + **shm-spring**. Free Studying Sims tab → live `simRegistry.ts`.
- Removed stale `questions_expmodel_backup_20260630` type block from `src/types/supabase.ts`.
- W0∥ DB shrink: DROP backup + VACUUM; gitignore import artifacts; prune IB preload.
- Scholaris vs SimuniVerse superiority plan (beyond graphics): 7 must-haves (predict-run-compare, misconception IDs, SAT/AP/IB tags, fidelity honesty labels, 12 deep labs not 40, SimTelemetry→Mistral, loading/empty/a11y+2D) + 5 wows (Kokoro TTS, whiteboard→tutor, lab→bank→study-plan loop, URL share state, probe/uncertainty literacy). SimuniVerse AI assistant is Devpost Phase 3 unshipped — Scholaris ships grounded tutor now. Do not fork 40 scenes; Sims under Free Studying. Canvas: `scholaris-vs-simuniverse-superiority.canvas.tsx`.
- STEM sim visual superiority research (vs typical Three.js/SimuniVerse demos): ACES+HDRI+ContactShadows over scene bloom; domain materials (MTM glass, SSS tissue, toon for discrete); postprocessing cinematic-only / never mobile composer; camera framing + ≤180ms param ease + trajectory trails; cobalt instrument chrome; 3 tiers cinematic/standard/lite; SimShell drei primitives list; 15-item visual bar checklist. Canvas: `stem-sim-visual-superiority-bar.canvas.tsx`.
- STEM sim multi-agent delivery plan: Phase A = **10 coherent tasks** (W0 contracts → W1 shell/catalog/kernel/CSP/tests → W2 four subject packs → W3 tutor+polish) mapped to user-facing **20 seats** (reviewers + pack subdirs); Phase B = **10** (integration, perf, a11y, curriculum tags, fidelity honesty, tutor E2E, mobile QA, docs, CI, commit). Anti-thrash: freeze contracts first; solo owners for `next.config.ts` / package.json / shell; packs only write `src/sims/<subject>/**`. Canvas: `stem-sim-multi-agent-delivery.canvas.tsx`.
- Chem/Bio/Math sim v1 vs SimuniVerse: recommend **8×3 exam-aligned sims** (15 P0), **2D-first** (only VSEPR needs optional Three.js). Chem P0: equilibrium, titration/buffers, kinetics, stoich, gases. Bio P0: enzymes, membrane/Ψ, central dogma, Punnett+χ², H–W. Math P0: deriv/int, slope fields, unit circle, sampling/CI, function transforms. Drop Mandelbrot/topology/3D cell tours. Canvas: `chem-bio-math-sim-v1.canvas.tsx`.
- STEM sim competitors vs SimuniVerse (PhET, Labster, Gizmos, CK-12, Desmos, GeoGebra, Brilliant): polish = curriculum + pedagogy + assessment + a11y + fidelity, not 3D wow. SimuniVerse = exploration gallery (40 sims, no standards/assess/teacher loop). 7 Scholaris differentiators: CED/exam-aligned catalog, predict→explain shells, transfer to scored items, misconception adaptive sequencing, Scho sim co-pilot, DOM/a11y twin, thin model-certified library. Canvas: `stem-sim-competitors-vs-simuniverse.canvas.tsx`.
- Physics sims v1 recommendation (SAT/AP/IB): Digital SAT = Math kinematics only (no Physics subject). Prioritize AP1+IB mechanics density + high-misconception AP2 (Lenz/circuits). Fidelity bar vs SimuniVerse: fixed-dt RK4/Verlet, ideal vs dissipative modes, probes/data/uncertainty, linked FBD+energy+graphs, predict→run residual, SimTelemetry→Mistral payload, 2D/a11y fallback — not poly count. v1 = 12 sims (P0: projectile, FBD, energy, collisions, SHM, UCM/orbits; P1: rotation, DC/RC, Faraday/Lenz, Snell; P2: PV thermo, Bernoulli). Defer double-slit/EM art/Doppler/nuclear spectacle. Canvas: `physics-sims-v1-recommendation.canvas.tsx`.
- STEM lab hub UI design research: OnePrep-shell polish vs SimuniVerse-style Three.js demos; cobalt (not purple); CSS Modules specs for experiment cards, control rail, readout meters, empty/WebGL fallbacks, motion budget. Canvas: `stem-lab-hub-ui-spec.canvas.tsx`.
- R3F educational sims research (Next.js App Router, 2025/2026): SimShell + SimKernel + dual Scene/Canvas2D; nuqs URL params; fixed-step physics (custom default, Rapier for collisions); a11y via DOM + @react-three/a11y; mobile tiers with contextlost→2D. Canvas: `r3f-edu-sim-architecture.canvas.tsx`.
- Interactive STEM sims plug-in map: Free Studying hub at `/dashboard/free-study` with Sims tab + live registry; Sparks = keyword suggest only in tutor; Desmos = practice/exam floating calc.
- SimuniVerse (Karushna fork) architecture audit: Next **15.4** + React 19 + R3F 9 / drei 10 / three 0.184; **40** catalog entries = 40 scene+page pairs + 40 App Router dirs (claim accurate on count). Physics flagships (pendulum/projectile/spring) = real RK4/ODE; biology (DNA etc.) = staged progress animations; gas laws = illustrative particle bounce + formula overlays. Dead deps leva/postprocessing; `physics.ts` almost unused; no dynamic `[slug]`, no tests/error/loading boundaries; thin a11y; homepage-only theme/favorites. Superior product must fix: plugin registry, shared integrators, fidelity tiers, WebGL/a11y hardening.
- Cloudflare secrets checklist (STEM 3D + R2 note images): for Next.js Scholaris, **Required** = Cloudflare account + R2 subscribe (payment method on file) + Account ID + bucket + R2 S3 Access Key ID/Secret (Object Read & Write / Bucket Item Write, bucket-scoped) + CORS; **Optional** = custom domain/CDN, r2.dev, Images transforms, Workers bindings, global CF API token for Wrangler; **Not needed** = Pages, Workers (if signing from Next.js), Images product, zone/DNS (unless public CDN), Stream/KV/D1. STEM 3D sims = client Three.js only (already in package.json) — no CF keys. Free usage within 10 GB / 1M Class A / 10M Class B / $0 egress but R2 activation needs CC. Scholaris keeps Supabase + Mistral BYOK; new secrets are R2_* only.
- SimuniVerse / sciencelab3d competing-product research (both MIT): code reuse allowed with copyright + MIT notice retention; experiment *ideas* alone generally need no attribution (copyright ≠ ideas); trademarks/branding separate. Recommended Scholaris path: inspiration only + original R3F sims (no vendored fork) for cleaner IP posture and product differentiation. Not legal advice.
- Installed Cloudflare agent setup: 11 skills under `~/.agents/skills/` (cloudflare, wrangler, workers-best-practices, etc.); added 5 MCP servers to `~/.cursor/mcp.json` (cloudflare, cloudflare-docs, cloudflare-bindings, cloudflare-builds, cloudflare-observability). Restart agent to load MCPs; OAuth on first Cloudflare tool use.
- Hybrid notes image storage research: confirmed Supabase Free Storage **1 GB** (uploads blocked when full); Pro 100 GB then **$0.0213/GB-mo**. Recommended **Cloudflare R2** (10 GB free, **$0 egress**) + Supabase Auth JWT gates + signed PUT/GET URLs; metadata in `free_study_note_assets` (object_key/provider). B2 = no-CC 10 GB fallback; Cloudinary 25 credits ≠ 25 GB. Migration via provider adapter (no UI rewrite). Canvas: `hybrid-notes-image-storage.canvas.tsx`.
- DB/repo bloat audit (live Supabase): DB **364 MB**; `questions` **341 MB** / 132k rows (heap 251 / indexes 29 / toast 61); vocab 2.3 MB; leftover `questions_expmodel_backup_20260630` 136 KB RLS-off. Repo: git-tracked import SQL/JSON ~**267–331 MB** under `scripts/data` + `english-questions.json`; local `.ap-ib-regen` 371 MB (gitignored); `src/data/tutor-preload` only **452 KB**. Prior compress migration already dropped 23 backups (500→348 MB). Ranked safe shrink opportunities delivered (no code changes).
- User greeted ("hi"); ready to help with Scholaris.
- Explored Scholaris UI/tutoring flow: entry/routing, CourseTutorHub, SSE tutor API, markdown rendering; confirmed whiteboard/voice/tutor-PDF are not implemented.
- Architecture audit (medium): Next.js 16 + npm; LLM is cloud-only (BYO Mistral / optional FreeModel central); no local inference (WebLLM/transformers/Ollama/ONNX); PDF text extract via pdfjs client-side for score reports; no whiteboard OCR, STT/TTS, or Tesseract; Gemini packages leftover for offline scripts only.
- Neural TTS research (in-browser, no SpeechSynthesis): WebGL ≠ TTS (graphics); real stack is WASM (CPU) + WebGPU (GPU) via ONNX Runtime / Transformers.js. Recommendation for Scholaris: `kokoro-js` + `onnx-community/Kokoro-82M-v1.0-ONNX` (voice `af_heart`), Web Worker, WebGPU/fp32 → WASM/q8 fallback; ~90–160MB first load then cached. Piper = speed/mobile fallback; SpeechT5 = older/robotic; Orpheus 3B = not browser-ready. Scholaris CSP must allow HF connect + wasm/'wasm-unsafe-eval' if self-hosting models.
- UX architecture exploration (Free Studying hub): mapped sidebar/navConfig/More flyout (no true account dropdown — profile → settings); tutor = CourseTutorHub at `/dashboard/courses/:id/tutor`; notes = localStorage QuestionNoteDialog only; PDF = score-report pdfjs path; recommended IA: main nav item + `/dashboard/free-study` hub with mode tabs; CSS fixes for sidebar width fallback / touch targets / account menu gap.
- Agent 1 DB shrink (backups/indexes): live DB still **365 MB**; no `*_backup_*` leftovers remain (prior DROP of 23 + `questions_expmodel_backup_20260630`); VACUUM ANALYZE `vocabulary_words` only; deferred unused indexes (~1.6 MB candidates) and `questions` toast/heap work to Agents 2–3.
